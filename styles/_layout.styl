$lineThickness = 2px

// reset
a, a:hover, a:visited,  a:active
  color inherit
  text-decoration none

body
  background $backgroundColor

header, footer
  text-align center
  padding 20px

.mainContainer
  display flex

.centerCol
  flex 14
  margin-left 20px
  margin-right 20px
  // border-left $lineThickness solid $lineColor
  // border-right $lineThickness solid $lineColor

.rightCol, .leftCol
  padding 20px

.rightCol
  flex 4
  
.leftCol
  flex 2
  color $hintFontColor
  headingBase()
  
section
  // padding 20px
  border-top $lineThickness solid $lineColor
  