$annotationWidth = 200px
$centerColMargin = 30px


.child_checkbox_container md-checkbox:last-of-type
  margin-left 0
  

$lineThickness = 2px

// reset
a, a:hover, a:visited,  a:active
  color inherit
  text-decoration none

// body, header, footer, .mainContainer, md-content
//   background $backgroundColor

header, footer
  text-align center
  padding 20px

.centerCol
  flex 14
  min-width 560px
  max-width 860px
  margin 0 $centerColMargin
  position relative
  overflow visible
  clearfix()

body
  padding 0 20px

.rightCol, .annotation
  width $annotationWidth

.annotation
  secondaryFont()
  position absolute
  right (-1 * $annotationWidth)
  
  &.inset
    // TODO figure this out
    right (-1 * $annotationWidth) - 50px
  p 
    margin-bottom 10px

.leftCol
  flex 2
  // color $hintFontColor
  headingBase()
  
section
  padding 20px
  // border-top $lineThickness solid $lineColor

.stdCard
  // background $cardColor !important;
  background rgba(52,152,219,0.2) !important;

// .questionMain
  